//递归,在函数内部调用自身
var count=0;
function say(){
	alert('从前有座山，山里有座庙，庙里有个老和尚再给小和尚讲故事，讲的内容是：从前有座山，山里有座庙，庙里有个老和尚再给小和尚讲故事。。。');
	count++;//1,2,3
	//当count为3的时候，结束函数执行
	if(count==3){
		return;
	}
	say();
}
say();